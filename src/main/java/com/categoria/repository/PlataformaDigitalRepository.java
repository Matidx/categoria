package com.categoria.repository;

import com.categoria.entity.PlataformaDigital;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlataformaDigitalRepository extends JpaRepository<PlataformaDigital, Long> {
}
